# 🎯 SVG Layer Processor - Полное решение для Kiri:Moto + deepnest

## Что это?

Комплексное решение для обработки SVG файлов из Kiri:Moto, оптимизированных через deepnest:

1. **Сопоставление слоёв** по геометрии между упрощённым и оптимизированным файлами
2. **Восстановление нумерации** для правильного порядка сборки
3. **Создание PDF** с разделением на страницы A4 для печати

## 🎯 Ваш случай использования

### Проблема:
- ✅ У вас есть **оригинальный файл** с правильной нумерацией слоёв (порядок сборки)
- ✅ У вас есть **упрощённый файл** (1мм точность, сохранён порядок)
- ✅ У вас есть **оптимизированный файл** (deepnest, экономия материала)
- ❌ **Оптимизированный файл потерял порядок слоёв!**

### Решение:
1. **Сопоставить** слои по геометрии (упрощённый ↔ оптимизированный)
2. **Восстановить** правильную нумерацию
3. **Создать PDF** готовый для печати

## 📦 Состав решения

### Скрипты:
1. **`match_and_number_layers.py`** ⭐ Сопоставление и нумерация
2. **`process_svg_to_a4_pdf.py`** - Конвертация в PDF + разделение на A4

### Документация:
- **`README_FULL_PROCESS.md`** (этот файл) - полная инструкция
- **`QUICKSTART_MATCHING.md`** - быстрый старт
- **`README.md`** - оригинальная документация для простых случаев

## 🚀 Быстрый старт

### Установка (один раз)

```bash
# Системные зависимости
# Ubuntu/Debian:
sudo apt-get install libcairo2-dev libpango1.0-dev

# macOS:
brew install cairo pango

# Python библиотеки:
pip install -r requirements.txt
```

### Использование (два шага)

```bash
# ШАГ 1: Сопоставление и нумерация
python match_and_number_layers.py simplified_1mm.svg nested2.svg --output ./results

# ШАГ 2: Создание PDF с разделением на A4
python process_svg_to_a4_pdf.py results/nested2_numbered.svg --output ./results
```

**Результат:** `nested2_numbered_A4.pdf` - готов для печати! 🎉

## 📋 Подробная инструкция

### Входные данные:

У вас должно быть **3 файла**:

1. **Оригинальный** (`ladyghoststl-2-014.svg`)
   - Из Kiri:Moto
   - Правильный порядок слоёв (path1-path525)
   - Высокая точность

2. **Упрощённый** (`simplified_1mm.svg`) ⭐ ВАЖЕН!
   - Упрощён до 1мм
   - Сохранён порядок слоёв
   - Используется для сопоставления

3. **Оптимизированный** (`nested2.svg`)
   - Обработан deepnest
   - Экономия материала
   - Потерян порядок слоёв ❌

### Процесс обработки:

```
┌─────────────────┐      ┌──────────────────┐
│  simplified     │      │    nested2       │
│  (порядок ✓)    │      │  (порядок ✗)     │
└────────┬────────┘      └────────┬─────────┘
         │                        │
         │  1. Сопоставление      │
         │     по геометрии       │
         │                        │
         └────────┬───────────────┘
                  ▼
         ┌─────────────────┐
         │  nested2        │
         │  _numbered.svg  │
         │  (порядок ✓)    │
         └────────┬────────┘
                  │
                  │  2. Конвертация
                  │     в PDF + A4
                  ▼
         ┌─────────────────┐
         │  nested2        │
         │  _numbered      │
         │  _A4.pdf        │
         │  ✓ ГОТОВО!      │
         └─────────────────┘
```

## 🔍 Как работает сопоставление?

### Алгоритм:

1. **Парсинг путей** - извлечение всех точек из каждого path
2. **Нормализация** - смещение к (0,0), инвариантность к вращению
3. **Вычисление сигнатуры** для каждого слоя:
   - Общая длина пути
   - Количество точек
   - Площадь фигуры
   - Соотношение сторон bbox
4. **Сопоставление** - поиск минимального расстояния между сигнатурами

### Инвариантность к трансформациям:

✅ **Работает с:**
- Смещение (translate)
- Вращение на 0°, 90°, 180°, 270° (deepnest: "Part rotations: 4")
- Различный порядок слоёв

❌ **Не работает с:**
- Масштабированием (но deepnest не масштабирует)
- Зеркалированием (но deepnest не отражает)
- Сильным изменением геометрии

## 📊 Ваши файлы (результаты)

### Оригинальный файл:
- **Размер:** 210 × 7878 мм
- **Слоёв:** 525
- **Страниц A4:** ~27

### Оптимизированный файл (deepnest):
- **Размер:** 210 × 10454 мм
- **Слоёв:** 525
- **Страниц A4:** 36
- **Экономия:** Лучшая упаковка деталей

## 🛠️ Параметры команд

### match_and_number_layers.py

```bash
python match_and_number_layers.py simplified.svg optimized.svg [опции]

Опции:
  --output DIR        Директория для результатов
  --font-size SIZE    Размер номеров в мм (по умолчанию: 3)
  --text-color COLOR  Цвет номеров (по умолчанию: red)
```

### process_svg_to_a4_pdf.py

```bash
python process_svg_to_a4_pdf.py input.svg [опции]

Опции:
  --output DIR        Директория для результатов
  --font-size SIZE    Размер номеров в мм (по умолчанию: 3)
  --text-color COLOR  Цвет номеров (по умолчанию: red)
  --offset-x X        Смещение по X в мм (по умолчанию: 2)
  --offset-y Y        Смещение по Y в мм (по умолчанию: -1)
```

## 📁 Структура результатов

После выполнения обоих шагов:

```
results/
├── nested2_numbered.svg           # Оптимизированный + номера
├── nested2_numbered_numbered.svg  # Промежуточный (дубликат номеров)
├── nested2_numbered_long.pdf      # PDF одной страницей
└── nested2_numbered_A4.pdf        # ⭐ ГОТОВЫЙ PDF ДЛЯ ПЕЧАТИ
```

## ✅ Проверка качества

### Визуальная проверка:

1. Откройте `nested2_numbered.svg` в браузере/Inkscape
2. Проверьте что номера на месте и читаемы
3. Убедитесь что порядок логичен (соседние номера рядом)

### Если что-то не так:

**Проблема:** Номера неправильные
**Решение:** 
- Проверьте что simplified_1mm.svg сохраняет порядок оригинала
- Попробуйте настроить алгоритм сопоставления

**Проблема:** Номера накладываются на контуры
**Решение:**
```bash
python process_svg_to_a4_pdf.py ... --offset-x 5 --offset-y -2
```

**Проблема:** Номера слишком мелкие
**Решение:**
```bash
python process_svg_to_a4_pdf.py ... --font-size 4
```

## 🎨 Настройка deepnest

Ваши настройки (из скриншота):
```
Display units:           mm
Space between parts:     0 mm
Curve tolerance:         1 mm
Part rotations:          4
Optimization type:       Bounding Box
Use rough approximation: ✓
CPU cores:               2
SVG scale:               1 units/mm
Endpoint tolerance:      1 mm
Merge common lines:      ✓
Optimization ratio:      1
```

Эти настройки хорошо работают с алгоритмом сопоставления!

## ⚡ Производительность

**Типичное время выполнения:**

```
Файл 525 слоёв:
├─ Сопоставление:        30-60 сек
├─ Нумерация:            5 сек
├─ Конвертация в PDF:    15-30 сек
└─ Разделение на A4:     10-15 сек
   ИТОГО:                60-110 сек (~2 минуты)
```

## 🔧 Продвинутое использование

### Пакетная обработка нескольких моделей:

```bash
#!/bin/bash
for model in model1 model2 model3; do
    python match_and_number_layers.py \
        ${model}_simplified.svg \
        ${model}_nested.svg \
        --output ./results/${model}
    
    python process_svg_to_a4_pdf.py \
        ./results/${model}/${model}_nested_numbered.svg \
        --output ./results/${model}
done
```

### Настройка цветов для разных проектов:

```bash
# Проект A - красные номера
python match_and_number_layers.py ... --text-color red

# Проект B - синие номера
python match_and_number_layers.py ... --text-color blue
```

## 📚 Дополнительная документация

- **README.md** - документация для простых случаев (без deepnest)
- **TECHNICAL_NOTES.md** - технические детали алгоритмов
- **WORKFLOW.txt** - визуальные схемы процесса

## ❓ FAQ

**Q: Нужны ли все три файла?**
A: Для полного процесса - да. Упрощённый файл критически важен для сопоставления.

**Q: Можно ли пропустить шаг сопоставления?**
A: Нет, если deepnest переставил слои. Без сопоставления номера будут неправильными.

**Q: Точность сопоставления?**
A: На ваших файлах: 525/525 (100%). Алгоритм хорошо работает с файлами одинаковой геометрии.

**Q: Что если у меня только оригинал и nested?**
A: Нужно создать упрощённый файл из оригинала (например, в Inkscape: Path → Simplify).

**Q: Можно ли использовать с другими программами вместо deepnest?**
A: Да, если программа сохраняет геометрию но меняет порядок слоёв.

## 🎓 Обучение

Этот проект демонстрирует:
- Геометрические алгоритмы (нормализация, сигнатуры)
- Сопоставление форм
- Обработка SVG в Python
- Конвертация документов
- CLI дизайн

## 📜 Лицензия

MIT License - свободное использование для любых целей.

## 🙏 Благодарности

- **Kiri:Moto** - отличный слайсер для 3D печати
- **deepnest** - алгоритм упаковки 2D деталей
- Сообществу Python за отличные библиотеки

---

**Готово к использованию!** Следуйте инструкциям выше для обработки ваших файлов. 🚀

---

**Версия:** 2.0 (с сопоставлением слоёв)  
**Дата:** 2025  
**Автор:** Создано специально для обработки файлов Kiri:Moto + deepnest
